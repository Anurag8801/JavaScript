alert(`I am javascript`)
// shows a message

prompt(`This is prompt window which takes input from user`, `this is default value which is optional`)
//It returns the text or, if Cancel button or Esc is clicked, null.

confirm(`This shows a question with options OK and cancel`)
//It returns true for OK and false for Cancel/Esc.